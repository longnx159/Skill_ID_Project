# Archive

This folder preserves previous source files, legacy code, old result folders, review artifacts, and package files for traceability.

Archived items are not read by the current pipeline. Put new data only in `input_data/` and write new results to `outputs/`.
