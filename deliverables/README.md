# Deliverables

`Skill_ID_Pipeline_v053.zip` is the runnable package containing the current folder-only pipeline, templates, documentation, and tests.

Regenerate the ZIP after changing runtime code or templates so the package stays in sync with the project folder.
